package utililties;

public class LoggerHelper {

	/*
	 * public static Logger logger; public Logger logGenerator() { String name =
	 * Thread.currentThread().getStackTrace()[2].getMethodName(); LoggerContext
	 * context = (LoggerContext) LogManager.getContext(false);
	 * context.setConfigLocation(new File("log4j2.xml").toURI()); logger =
	 * LogManager.getLogger(name); return logger; }
	 */
}
